# Requirements pour le téléchargement et l'analyse des députés français
# Installez avec: pip install -r requirements.txt

# Core dependencies
pandas>=1.5.0
requests>=2.28.0

# Pour les analyses et visualisations (optionnel)
matplotlib>=3.6.0
seaborn>=0.12.0

# Pour les notebooks Jupyter (optionnel)
jupyter>=1.0.0
ipywidgets>=8.0.0

# Pour l'export Excel (optionnel)
openpyxl>=3.0.0

# Pour une meilleure gestion des CSV (optionnel)
chardet>=5.0.0